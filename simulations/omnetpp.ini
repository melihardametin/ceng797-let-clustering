[General]
network = src.letclustering.LetNetwork

*.numHosts = 5

*.host[*].numApps = 1
*.host[*].app[0].typename = "LetApp"
*.host[*].app[0].localPort = 5000
*.host[*].app[0].destPort = 5000

*.host[*].mobility.typename = "LinearMobility"
*.host[*].mobility.speed = 10mps
*.host[*].mobility.constraintAreaMinX = 0m
*.host[*].mobility.constraintAreaMinY = 0m
*.host[*].mobility.constraintAreaMaxX = 500m
*.host[*].mobility.constraintAreaMaxY = 500m