import inet.common.INETDefs;
import inet.common.packet.chunk.Chunk;

namespace inet;


class LetHello extends FieldsChunk {
    double positionX;
    double positionY;
    double speed;
    double angle;
}